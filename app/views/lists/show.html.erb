<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @list.name %>
</p>

<p>
  <b>Owner:</b>
  <%= @list.owner_id %>
</p>

<p>
	<b>Tasks</b>
	<ul>
	<% @list.tasks.each do |t| %>
		<li><%= t.description %></li>
	<% end %>
	</ul>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
