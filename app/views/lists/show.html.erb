<p id="notice"><%= notice %></p>

<div id="worked"></div>

<p>
  <b>Name:</b>
  <%= @list.name %>
</p>

<p>
  <b>Owner:</b>
  <%= @list.owner_id %>
</p>

<p>
	<b>Tasks</b>
	<ul>
	<% @list.tasks.each do |t|%>
<<<<<<< HEAD
		<br />
		<% if t.status == false %>
		    <%= check_box_tag t.id, true, checked = false   %>
		    <%= observe_field t.id, :url => { :controller => 'tasks', :action => :finish , :id => t.id },
		        :frequency => 0.25,
		        :update => :status,
		        :with => true
		    %>
		<% end %>
		<%= link_to t.description , t %>
=======
		<li>
			<% if t.status == "unfinished" %>
			    <%= check_box_tag t.id, true, checked = false, {:class => 'task'} %>
			<% else %>
				<%= check_box_tag t.id, true, checked = true, {:class => 'task'} %>
			<% end %>
			<%= link_to t.description , t %>
		</li>
>>>>>>> I think this would do something if we had jquery and got rid of prototype. Not entirely sure. We get checkboxes now, though.
	<% end %>
	</ul>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>